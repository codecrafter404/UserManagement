prototype(Sandstorm.UserManagement:Component.ResetPassword.EmailSent) < prototype(Neos.Fusion:Component) {

    resetPasswordFlow = ''

    renderer = afx`
        <div class="callout success">
            <p>
              Eine Email wurde an <strong>{resetPasswordFlow.email}</strong> versendet,
              falls dieser Account existiert. Klicken Sie auf
              den enthaltenen Link, um Ihr Passwort zurückzusetzen.
            </p>
        </div>
    `
}
